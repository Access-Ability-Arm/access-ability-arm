# Access Ability Arm - Monorepo Package Installation
# This file installs all three packages in development mode
# Dependencies are managed in each package's pyproject.toml

# Core package (configuration, hardware, workers)
-e packages/core

# Vision package (RF-DETR, YOLOv11, face detection)
-e packages/vision

# GUI package with Flet interface (cross-platform)
-e packages/gui[flet]

# Lite6 robotic arm driver (UFactory xArm SDK)
-e packages/lite6_driver

# Optional: Install with PyQt6 GUI instead
# -e packages/gui[pyqt]

# Optional: Install both GUI frameworks
# -e packages/gui[all]

# Optional: Development dependencies
# -e .[dev]
