# GUI Frameworks
# PyQt6>=6.6.0  # Legacy GUI (deprecated - use Flet instead)
flet>=0.24.0  # Modern cross-platform UI framework

# Computer Vision
opencv-python-headless>=4.8.0  # Headless version saves ~60-70MB vs opencv-python
mediapipe>=0.10.0

# Object Detection & Segmentation
git+https://github.com/roboflow/rf-detr.git  # RF-DETR Seg (SOTA Nov 2025, 44.3 mAP)
ultralytics>=8.3.0  # YOLOv11-seg (fallback option)

# Numerical Computing
numpy>=1.24.0

# Development Tools
ipykernel>=7.0.0

# RealSense Camera Support (for main-rd.py only)
# pyrealsense2>=2.54.0
# NOTE: pyrealsense2 must be installed manually from source (v2.56.5)
# Follow installation guide: https://github.com/IntelRealSense/librealsense/blob/master/doc/installation.md
# Only required if using main-rd.py with Intel RealSense camera
